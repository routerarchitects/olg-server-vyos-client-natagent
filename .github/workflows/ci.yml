name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-and-build:
    name: Test And Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Check formatting
        run: |
          UNFORMATTED=$(gofmt -l $(find . -type f -name '*.go' -not -path './.git/*'))
          if [ -n "$UNFORMATTED" ]; then
            echo "The following files are not gofmt-formatted:"
            echo "$UNFORMATTED"
            exit 1
          fi

      - name: Run unit tests
        run: go test ./...

      - name: Build
        run: go build ./...

  smoke-tests:
    name: Smoke Tests
    runs-on: ubuntu-latest
    needs: test-and-build
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Install nats-server
        run: |
          go install github.com/nats-io/nats-server/v2@v2.10.22
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Make scripts executable
        run: chmod +x tests/scripts/*.sh

      - name: Validate config script
        run: ./tests/scripts/validate-config.sh

      - name: Phase 3 configure smoke
        timeout-minutes: 5
        run: NATS_PORT=4223 ./tests/scripts/phase3-real-nats-configure-smoke.sh

      - name: Phase 4 action smoke
        timeout-minutes: 5
        run: NATS_PORT=4224 ./tests/scripts/phase4-real-nats-action-smoke.sh
